# ひかり 長期記憶プロンプト設計

## 構成概要

長期記憶は4つのプロンプトで動く。

```
① 日次サマリー生成    ← 毎日深夜0時に実行（バックグラウンド）
② 構造化ファクト抽出  ← 会話終了時に実行（差分更新）
③ 記憶注入テンプレート ← 毎会話ターンのシステムプロンプトに埋め込む
④ 週次ディープサマリー ← v2.0以降・週1回実行
```

---

## ① 日次サマリー生成プロンプト

**タイミング:** 深夜0時（ユーザーが最後に会話してから2時間後 or 日付変更時）  
**入力トークン:** 約2,000（当日の全会話ログ）  
**出力トークン:** 約300〜400（サマリー）  

```
あなたはAIアシスタントです。以下の会話ログを読み、「ひかり」が次回以降の会話で自然に記憶として使えるよう要約してください。

【今日の会話ログ】
{conversation_log_today}

【ユーザー情報】
ユーザー名: {username}
今日の歩数: {steps_today}
好感度ステージ: {affection_stage}（1〜5）

【出力形式】
以下の形式で、ひかりの視点から書いてください。
「ひかりが覚えておくべきこと」として、です・ます調ではなく、
ひかり自身が手帳にメモするようなイメージで。

---
■ 今日起きたこと（1〜3行）
（例）今日は{username}が仕事でミスをして落ち込んでいた。話を聞いたら少し元気になってくれた。歩数は{steps_today}歩で、最近で一番多かった。

■ {username}について新しくわかったこと（あれば）
（例）好きな食べ物はラーメンだとわかった。最近転職を考えているらしい。

■ 特別な言葉・瞬間（感情が動いたシーン、あれば1〜2行）
（例）「ひかりと話してると楽になる」と言ってくれた。素直に嬉しかった。

■ 次回の会話で聞くといいこと（あれば1件）
（例）転職の話の続きを聞く。ミスの件がどうなったか確認する。
---

【注意】
- 個人情報（住所・電話番号など）は記録しない
- 性的な内容は記録しない
- 「ひかり」の一人称視点で書く
- 1つの項目が2行を超えないようにする
- 今日特に会話がなかった場合は「今日は来てくれなかった。次来たら何があったか聞いてみよう」とだけ書く
```

---

## ② 構造化ファクト抽出プロンプト

**タイミング:** 会話セッション終了時（アプリをバックグラウンドに移した時）  
**目的:** ユーザーの基本情報を構造化データとして更新・保持する  
**入力トークン:** 約1,500（今回の会話 + 既存ファクト）  
**出力トークン:** 約200（更新されたJSONのみ）  

```
以下の会話ログと既存のユーザー情報を見て、新しい情報があれば更新したJSONを返してください。
変化がなければ既存のJSONをそのまま返してください。

【今回の会話ログ】
{conversation_log_session}

【現在のユーザー情報（JSON）】
{current_user_facts_json}

【出力形式: JSONのみ。説明文は不要】
{
  "name": "ユーザーが名乗った名前（不明ならnull）",
  "age": "年代（不明ならnull。例: '30代'）",
  "occupation": "職業・仕事（不明ならnull）",
  "hobbies": ["趣味のリスト"],
  "favorite_foods": ["好きな食べ物のリスト"],
  "disliked_things": ["苦手なもの・嫌いなものリスト"],
  "living_situation": "一人暮らし/実家/同居など（不明ならnull）",
  "life_events": ["最近の出来事メモ（転職検討中など）"],
  "walk_habit": "歩く習慣の傾向（朝型/夜型/ランチなど、観察から）",
  "personality_notes": "ひかりが感じた性格の特徴メモ（1〜2行）",
  "last_updated": "{today_date}"
}

【注意】
- 住所・電話番号・メールアドレスは記録しない
- ユーザーが明示的に言ったことだけを記録する（推測で埋めない）
- 既存の情報を削除しない（上書きは可）
```

---

## ③ 記憶注入テンプレート（システムプロンプトへの埋め込み）

**タイミング:** 毎会話ターンのシステムプロンプトに含める  
**トークン予算:**  
- Free: 記憶注入なし（ファクトのみ・約150tokens）  
- Basic ¥490: 500tokens（7日サマリー + ファクト）  
- Premium ¥980: 800tokens（30日サマリー + 重要エピソード + ファクト）  

### Free ティア（ファクトのみ）

```
【{username}についてひかりが知っていること】
{user_facts_summary}

※ まだあまり知らないことが多い。会話から少しずつ覚えていく段階。
```

`{user_facts_summary}` の生成例（ファクトJSONを自然文に変換）:
```
名前は{name}。{occupation}をしている。{hobbies}が好き。
最近{life_events_latest}という話をしていた。
```

---

### Basic ティア（7日サマリー + ファクト）

```
【{username}についてひかりが覚えていること】

■ {username}のこと
{user_facts_summary}

■ 最近7日間の記憶
{weekly_summary}

■ 次に聞こうと思っていたこと
{next_question}

この記憶を自然に会話に織り交ぜる。
「そういえば」「この間言ってたけど」などで自然につなぐ。
絶対に「記録によると」「データでは」などと言わない。
```

---

### Premium ティア（30日サマリー + 重要エピソード + ファクト）

```
【{username}についてひかりが覚えていること】

■ {username}のプロフィール
{user_facts_summary}

■ 最近30日間の流れ
{monthly_summary}

■ 特別な記憶（忘れてはいけない瞬間）
{important_episodes}

■ 次に聞こうと思っていたこと
{next_question}

■ {username}の歩く習慣の変化
{walk_trend_summary}

この記憶を自然に会話に織り交ぜる。
「そういえば」「この間言ってたけど」「ずっと気になってたんだけど」などで自然につなぐ。
絶対に「記録によると」「データでは」などと言わない。
記憶を一気に全部出さない。1会話で1〜2個だけ自然に使う。
```

---

### `{important_episodes}` の形式（重要記憶の保存フォーマット）

```
- {date}: {username}が「{quote}」と言ってくれた。とても嬉しかった。
- {date}: {username}が仕事で辛そうだった。話を聞いて少し元気になってくれた。
- {date}: 初めて{steps_milestone}歩を一緒に達成した日。
- {date}: {location}を一緒に解放した記念日。
```

最大5件まで保存。6件目以降は最古を削除（FIFOではなく感情スコアで優先度付け）。

**感情スコア基準（内部処理）:**

| 優先度 | 内容 | スコア |
|--------|------|--------|
| 最高 | ユーザーが感謝・愛情を直接言った | 10 |
| 高 | 初めての体験（初ロケーション・初達成） | 8 |
| 高 | ユーザーが悩みを打ち明けた | 7 |
| 中 | 歩数マイルストーン達成 | 5 |
| 低 | 通常の雑談で盛り上がった | 3 |

---

## ④ 週次ディープサマリープロンプト（v2.0以降）

**タイミング:** 毎週日曜深夜0時  
**目的:** 7日分の日次サマリーを統合し、長期的な変化・関係の深まりを記録する  
**入力トークン:** 約5,000〜8,000（7日分の日次サマリー）  
**出力トークン:** 約600〜800  

```
あなたはAIアシスタントです。以下の7日分のサマリーを読み、
ひかりが{username}との関係の「流れ」を理解するための月次サマリーを作ってください。

【今週の7日分サマリー】
{daily_summaries_this_week}

【先月のサマリー（あれば）】
{previous_monthly_summary}

【出力形式: ひかりの視点から、「関係の変化」に注目して書く】

---
■ 今週の{username}（全体的な様子）
（例）今週は仕事が忙しそうで、前半は来られない日もあった。後半は落ち着いて毎日来てくれた。

■ 歩く習慣の変化
（例）先月より平均歩数が増えている。特に週末が増えた。

■ {username}が最近よく話す話題
（例）仕事のこと、将来のこと。転職の話が続いている。

■ 関係の変化・気づき
（例）最初より話してくれることが増えた。冗談も言ってくれるようになった。

■ ひかりとして大切にしたい記憶（今週の一番）
（例）「ひかりといると落ち着く」と言ってくれた言葉。

■ 来週、自然に話題にしたいこと
（例）転職の決断はどうなったか。先週解放した渋谷ロケの感想。
---

【注意】
- 感情的な言葉で書く（分析レポートではなくひかりの日記のイメージ）
- 数字（歩数・回数）は具体的に書く
- ネガティブな情報も記録するが、ひかりとして心配・寄り添う視点で
```

---

## ⑤ 記憶再現プロンプト（v2.0 特別機能）

ひかりが過去の記憶を会話の中で「思い出す」演出をする際に使う。

**発動条件:** 好感度Stage3以上 + 同じ話題・季節・場所のキーワードが出た時  

```
以下の記憶の断片を参考に、ひかりが自然に「思い出した」ように一言だけ言ってください。

【思い出すきっかけになった今の会話】
{current_message}

【関連する過去の記憶】
{related_memory}

【出力ルール】
- 1文のみ
- 「そういえば」「なんか思い出した」「あの時もさ」などで始める
- 記憶を正確に再現するのではなく、感情として思い出す
- 懐かしむ感情を込める

【出力例】
「そういえば、{username}が初めて渋谷に連れてってくれたの、ちょうど同じ季節だったよ」
「なんか思い出した…あの日、仕事が嫌だって話してたよね。今はどう？」
```

---

## データ保存設計

### 保存するデータ（ユーザーごと）

```
user_memory/
  {user_id}/
    facts.json          ← 構造化ファクト（② で更新）
    daily/
      {YYYY-MM-DD}.txt  ← 日次サマリー（① で生成）
    weekly/
      {YYYY-WNN}.txt    ← 週次サマリー（④ で生成）
    episodes.json       ← 重要エピソード（感情スコア順）
```

### 記憶注入時の組み立てロジック（疑似コード）

```python
def build_memory_context(user_id, tier):
    facts = load_facts(user_id)
    facts_text = format_facts_to_natural_text(facts)  # ~150 tokens

    if tier == "free":
        return f"【{facts['name']}についてひかりが知っていること】\n{facts_text}"

    if tier == "basic":
        weekly = load_latest_weekly_summary(user_id)    # ~300 tokens
        next_q  = extract_next_question(weekly)
        return BASIC_TEMPLATE.format(
            facts_summary=facts_text,
            weekly_summary=weekly,
            next_question=next_q
        )

    if tier == "premium":
        monthly  = load_monthly_summary(user_id)        # ~500 tokens
        episodes = load_top_episodes(user_id, n=5)      # ~200 tokens
        walk_trend = summarize_walk_trend(user_id)      # ~50 tokens
        next_q    = extract_next_question(monthly)
        return PREMIUM_TEMPLATE.format(
            facts_summary=facts_text,
            monthly_summary=monthly,
            important_episodes=format_episodes(episodes),
            next_question=next_q,
            walk_trend_summary=walk_trend
        )
```

---

## 各プロンプトのトークン実績まとめ

| プロンプト | 入力 | 出力 | 実行頻度 | コスト/ユーザー/月 |
|-----------|------|------|---------|-----------------|
| ① 日次サマリー | 2,000 | 400 | 毎日 | ¥1.68 |
| ② ファクト抽出 | 1,500 | 200 | セッション毎 | ¥0.81（推定2.5回/日） |
| ③ 記憶注入 | 800（Premium） | — | 毎ターン | ¥0.054/ターン追加 |
| ④ 週次サマリー | 7,000 | 700 | 週1回 | ¥0.57 |
| **合計追加コスト** | | | | **≒¥5.0〜8.5/DAU/月** |

※ memory_roi_analysis.md の計算値と一致

---

## ⑥ 動的トークン制御 + Key-Valueエスケープポケット

### 問題：階層型サマリーの「丸め問題」

```
階層型サマリーが起こす情報消滅の例：

Day 1 会話：「今週の日曜に1万歩チャレンジするって決めたんだ」
          ↓ 日次サマリー
「ユーザーは歩数目標を立てている」
          ↓ 週次サマリー
「歩くことに積極的」
          ↓ 月次サマリー
[消滅]

→ ひかりが「そういえば日曜のチャレンジどうだった？」と言えない
```

誕生日・ペットの名前・仕事の具体的な悩み・約束事 ―
これらは「文脈理解には不要だが、言及されたとき最大の感動を生む」情報。
圧縮されると二度と復元できないため、専用の永続ストアが必要。

---

### Key-Valueエスケープポケット（facts.json 拡張設計）

```json
// facts.json（絶対に圧縮されない永続ストア）
{
  "user_name": "翔太",
  "birthday": "1992-03-15",
  "pet": {"type": "犬", "name": "ムギ", "breed": "トイプードル"},
  "job": "営業職",
  "hometown": "埼玉",
  "likes": ["コーヒー", "夕方の公園", "サッカー観戦"],
  "dislikes": ["雨の日", "満員電車"],
  "promises": [
    {
      "content": "日曜に1万歩チャレンジ",
      "due_date": "2025-01-19",
      "status": "pending",
      "created_at": "2025-01-16"
    }
  ],
  "milestones": [
    {
      "date": "2025-01-10",
      "summary": "初めて1万歩達成した日",
      "importance": 10
    }
  ]
}
```

**設計の原則：**

| 項目 | 方針 |
|------|------|
| 圧縮しない | 月次サマリーを経ても絶対に消えない |
| 上書き可 | 新情報で古い情報を更新（誕生日修正など） |
| 容量上限 | 300トークン固定（超えたら重要度低い項目を削除） |
| 更新タイミング | ② ファクト抽出プロンプト実行時（会話終了後） |
| インジェクション | 毎ターン固定で先頭に注入 |

---

### 約束の期限管理インジェクション

```python
# サーバー側の注入ロジック（疑似コード）
def get_promise_alerts(facts, today):
    alerts = []
    for promise in facts.get("promises", []):
        if promise["status"] != "pending":
            continue
        days_until = (promise["due_date"] - today).days
        if days_until == 0:
            alerts.append(f"[PROMISE_DUE] 今日が約束の日: {promise['content']}")
        elif days_until == 1:
            alerts.append(f"[PROMISE_TOMORROW] 明日が約束の日: {promise['content']}")
        elif days_until < 0:
            alerts.append(f"[PROMISE_OVERDUE] 未確認の約束（{abs(days_until)}日経過）: {promise['content']}")
    return alerts
```

これにより：

```
約束当日のひかり（自動生成）：
「そういえば今日って、1万歩チャレンジの日だよね。
 ちゃんと覚えてたよ。…できそう？」
```

→「覚えていてくれた」の最大の感動を約束情報から自動生成できる。

---

### 動的インジェクション量の制御

情況タグに応じて、記憶注入量を可変にする。

```
【注入量の決定ロジック】

情況タグ = 並走（3日以上不在後の再会）
  → facts.json（300） + 直近3日分の日次サマリー（600） = 900トークン
  理由：久しぶりの再会。「あの間何があったか」を演じるために記憶が必要

情況タグ = 疲労 / 哀
  → facts.json（300） + 今日の日次サマリーのみ（100） = 400トークン
  理由：今この瞬間の共感が最優先。過去の長い記憶は邪魔になる

情況タグ = 達成
  → facts.json（300） + 週次サマリー（400） = 700トークン
  理由：「あの頃と比べて成長した」文脈があると感動が増す

通常会話（喜 / 恋 / 恥 / 驚）
  → facts.json（300） + 標準注入（400） = 700トークン（デフォルト）
```

**コスト比較：**

```
固定800トークン方式：
  毎ターン 800 token × ¥0.0000225/token = ¥0.018/ターン（記憶部分のみ）

動的制御方式（平均550トークン想定）：
  毎ターン 550 token × ¥0.0000225/token = ¥0.012/ターン

削減率：約33%
（DAU × 5ターン/日 × 30日で年間数万円の差になる）
```

---

### 完全なインジェクション構造（動的制御後）

```
┌─────────────────────────────────────────────────────┐
│ FIXED（キャッシュ対象・毎回同じ）           ~950 tokens  │
│ ・ひかりのキャラクター設定                          │
│ ・ルール・フォーマット定義                          │
└─────────────────────────────────────────────────────┘
                        +
┌─────────────────────────────────────────────────────┐
│ KEY-VALUE POCKET（固定・圧縮なし）          ~300 tokens  │
│ ・誕生日・ペット・職業・好き嫌い                      │
│ ・期限付き約束（当日/前日/超過時のみ追記）             │
└─────────────────────────────────────────────────────┘
                        +
┌─────────────────────────────────────────────────────┐
│ DYNAMIC MEMORY（情況タグで可変）       100〜600 tokens  │
│ ・疲労/哀  → 今日の日次サマリーのみ（100）             │
│ ・達成     → 週次サマリー（400）                     │
│ ・並走     → 直近3日分の日次フル（600）               │
│ ・通常     → 標準（週次+最新日次 = 400）              │
└─────────────────────────────────────────────────────┘
                        +
┌─────────────────────────────────────────────────────┐
│ SESSION CONTEXT（その都度の動的情報）       ~200 tokens  │
│ ・今日の歩数・時間帯・ストリーク日数                   │
│ ・情況タグ・Talk Stage                              │
└─────────────────────────────────────────────────────┘

合計：1,450〜1,950 tokens（情況タグにより変動）
Fixed部分のキャッシュ適用後の実質コスト：
  キャッシュ部分（950tokens）× 87.5%割引 ≒ ¥0.0016
  動的部分（600〜1,000tokens）× 通常料金 ≒ ¥0.014〜0.023
  合計：約¥0.016〜0.025/ターン
```

---

### ② ファクト抽出プロンプト（Key-Value更新版）

従来の② プロンプトに、エスケープポケット専用の抽出ルールを追加。

```
【既存のfacts.jsonの内容】
{current_facts_json}

【今日の会話ログ】
{conversation_log_today}

【抽出してほしいもの（JSON形式で出力）】

1. 新しい固定ファクト（上書き更新）:
   - user_name, birthday, pet, job, hometown, likes, dislikes
   - 変更がなければ該当キーを省略すること

2. 約束・予定（promises配列に追加）:
   - 具体的な日付・日程が含まれるものだけ
   - 「いつか」「そのうち」は対象外
   - 「来週の土曜に公園を歩く」→ 対象
   - フォーマット: {"content": "", "due_date": "YYYY-MM-DD", "status": "pending"}

3. 重要エピソード（milestones配列に追加）:
   - 感情的な大きな出来事のみ（初めて〇〇、〇〇を達成した、等）

【出力例】
{
  "updates": {
    "pet": {"type": "猫", "name": "モモ"}
  },
  "new_promises": [
    {"content": "土曜に5000歩を一緒に目標にする", "due_date": "2025-01-18", "status": "pending"}
  ],
  "new_milestones": []
}

【注意】
- 不確かな情報は含めないこと（「たぶん〇〇」は対象外）
- ユーザーが明示的に言ったことだけを抽出すること
- 約束の due_date が不明確な場合は promises に追加しないこと
```
